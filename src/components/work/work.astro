---
import Case from "./case.astro";
import { casearray } from "../../scripts/casearray";
---

<div>
  {
    casearray.map((c) => (
      <Case
        href={`${c.slug}`}
        video={c.video}
        titel={c.titel}
        tekst={c.displayText}
        programs={c.programs}
      />
    ))
  }
</div>
